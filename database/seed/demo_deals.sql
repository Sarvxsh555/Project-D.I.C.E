-- TODO: seed demo deals once customer/product ids are known at insert time.
-- Left as a placeholder; the OEEG scenario runner creates demo deals live
-- via the API instead, so seeding one here is optional. See oeeg/scenarios/.
SELECT 1;

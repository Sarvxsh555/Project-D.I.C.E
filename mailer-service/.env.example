PORT=4000

# Leave SMTP_HOST unset to auto-create a free Ethereal test inbox (no real email sent,
# a preview link is logged to the console instead). Set these to use real SMTP (e.g. Gmail app password).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@example.com
